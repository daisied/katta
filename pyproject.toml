[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [
  "app/data",
  "app/plugins",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
